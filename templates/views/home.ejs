<section class="hero py-16">
    <div class="container mx-auto flex items-center justify-between">
        <div class=" ml-10 w-1/2">
            <h6 class="text-lg"><em>Are you free ?</em></h6>
            <h1 class="text-6xl font-bold">Don't wait !</h1>
            <a href="/products"><button class="px-6 py-2 rounded-full text-white font-bold mt-4 btn-primary">Order Now</button></a>
        </div>
        <div class="w-1/2 mr-5">
            <img src="/img/1.jpg">
        </div>
    </div>
</section>

<!-- <section class="menu container mx-auto py-8">
    <h1 class="ml-10 text-xl font-bold mb-8">All Product</h1>
    <div class="grid grid-cols-5 gap-14">

        <% products.forEach(function(product){ %>
            <div class="w-64">
                <img class="h-40 mb-4 mx-auto" src="/img/<%= product.image%>" alt="pizzaImg">
                <div class="text-center">
                    <h2 class="mb-4 text-lg"><%=product.name%></h2>
                    <div class="flex items-center justify-around mt-6">
                        <span class="font-bold text-lg">Rs. <%= product.price %></span>
                        <button data-product="<%= JSON.stringify(product) %>" class="addToProduct py-1 px-6 rounded-full flex items-center font-bold">
                            <a href="/success-buy"><span>Buy</span></a>
                        </button>
                    </div>
                </div>
            </div>
    <% }) %>
        
    </div>
</section>
     -->








